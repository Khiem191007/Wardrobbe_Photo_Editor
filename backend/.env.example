# Get your free API key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here
PORT=3000

# Optional: comma-separated list of origins allowed via CORS
# Leave blank to disable CORS (same-origin only)
CORS_ORIGINS=
